{
  "routes": [
    {
      "method": "GET",
      "path": "/cost-centers",
      "handler": "cost-centers.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cost-centers/count",
      "handler": "cost-centers.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/cost-centers/:id",
      "handler": "cost-centers.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/cost-centers",
      "handler": "cost-centers.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/cost-centers/:id",
      "handler": "cost-centers.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/cost-centers/:id",
      "handler": "cost-centers.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
